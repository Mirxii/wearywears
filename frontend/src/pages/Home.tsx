import { Container, Typography } from '@mui/material';

const Home = () => {
  return (
    <Container>
      <Typography variant="h2">Home</Typography>
    </Container>
  );
};

export default Home;
